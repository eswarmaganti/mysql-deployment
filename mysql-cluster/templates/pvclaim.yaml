apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{- include "mysql-cluster.fullname"}}-pvclaim"
  labels:
    {{- include "mysql-cluster.labels" | nindent 4}}
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi