---
# Source: mysql-cluster/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mysql-cluster
  labels:
    helm.sh/chart: mysql-cluster-0.1.0
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
automountServiceAccountToken: true
---
# Source: mysql-cluster/templates/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: "mysql-cluster-docker-config-secret"
  labels:
    helm.sh/chart: mysql-cluster-0.1.0
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm

type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "kjagfkjgsfk"
---
# Source: mysql-cluster/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-cluster-conf
  labels:
    helm.sh/chart: mysql-cluster-0.1.0
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
data:
  primary.cnf: |
    # Apply this config only on the primary node
    [mysqld]
    log-bin
  replica.cnf: |
    # Apply this config only on replicas
    [mysqld]
    super-read-only
---
# Source: mysql-cluster/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-conf
  labels:
    helm.sh/chart: mysql-cluster-0.1.0
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
data:
  MYSQL_ALLOW_EMPTY_PASSWORD: "1"
---
# Source: mysql-cluster/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: mysql-cluster
  labels:
    helm.sh/chart: mysql-cluster-0.1.0
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  clusterIP: None
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
  selector:
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
---
# Source: mysql-cluster/templates/service.yaml
# Client service for connecting to any MYSQL instance for reads.
# For writes, you must instead connect to the primary

apiVersion: v1
kind: Service
metadata:
  name: mysql-cluster-read
  labels:
    helm.sh/chart: mysql-cluster-0.1.0
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
    readonly: true
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
  selector:
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
---
# Source: mysql-cluster/templates/statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-cluster
  labels:
    helm.sh/chart: mysql-cluster-0.1.0
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: mysql-cluster
      app.kubernetes.io/instance: mysql-cluster
  template:
    metadata:
      labels:
        helm.sh/chart: mysql-cluster-0.1.0
        app.kubernetes.io/name: mysql-cluster
        app.kubernetes.io/instance: mysql-cluster
        app.kubernetes.io/version: "1.16.0"
        app.kubernetes.io/managed-by: Helm
    spec:
      imagePullSecrets:
        - name: "mysql-cluster-docker-config-secret"
      serviceAccountName: "mysql-cluster"
      securityContext:
        fsGroup: 999
      initContainers:
        - name: init-mysql
          image: "mysql:9"
          imagePullPolicy: IfNotPresent
          command:
            - bash
            - -c
            - |
              set -ex
            
              # Generate mysql server-id from pod ordinal index.
              [[ $HOSTNAME =~ -([0-9]+)$ ]] || exit 1
              ordinal=${BASH_REMATCH[1]}
              echo [mysqld] > /mnt/conf.d/server-id.cnf
            
              # Add an offset to avoid reserved server-id=0 value.
            
              echo server-id=$((100 + $ordinal)) >> /mnt/conf.d/service-id.cnf
            
              # Copy appropriate conf.d files from config-map to emptyDir.
              if [[ $ordinal -eq 0 ]]; then
                cp /mnt/config-map/primary.cnf /mnt/conf.d/
              else
                cp /mnt/config-map/replica.cnf /mnt/conf.d/
              fi
          volumeMounts:
            - mountPath: /mnt/conf.d
              name: conf
            - mountPath: /mnt/config-map
              name: config-map

        - name: xtrabackup
          image: "percona/percona-xtrabackup:8.4"
          imagePullPolicy: IfNotPresent
          command:
            - bash
            - -c
            - |
              set -ex
            
              # skip the clone if data already exists.
              [[ -d /var/lib/mysql/mysql ]] && exit 0
            
              # skip the clone on primary (ordinal index 0)
              [[ `hostname` =~ -([0-9]+)$ ]] || exit 1
              ordinal=${BASH_REMATCH[1]}
              [[ $ordinal -eq 0 ]] && exit 0
            
              # clone the data from previous peer.
            
              #ncat --recv-only mysql-$(($ordinal-1)).mysql 3307 | xbsstream -x -C /var/lib/mysql
              socat TCP:mysql-$((${ordinal}-1)).mysql:3307 EXEC:"xbstream -x -C /var/lib/mysql"
            
              # Prepare the backup
              xtrabackup --prepare --target-dir=/var/lib/mysql
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: data
            - mountPath: /etc/mysql/conf.d
              name: conf

      containers:
        - name: mysql-cluster
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            runAsNonRoot: true
            runAsUser: 999
          image: "mysql:9"
          imagePullPolicy: IfNotPresent
          ports:
            - name: mysql
              containerPort: 3306
              protocol: TCP
          livenessProbe:
            exec:
              command:
              - mysqladmin
              - ping
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command:
              - mysql
              - -h
              - 127.0.0.1
              - -e
              - SELECT 1
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
          volumeMounts:
            - mountPath: /etc/mysql/conf.d
              name: conf
            - mountPath: /var/lib/mysql
              name: data
              subPath: mysql

        - name: xtrabackup
          image: "percona/percona-xtrabackup:8.4"
          imagePullPolicy: IfNotPresent
          ports:
            - name: xtrabackup
              containerPort: 3307
          command:
            - bash
            - -c
            - |
              set -ex
              cd /var/lib/mysql
            
              # Determine binlog position of cloned data, if any.
            
              # Check if the `xtrabackup_slave_info` file is present & it's not empty
              if [[ -f xtrabackup_slave_info && "x$(<xtrabackup_slave_info)" != "x" ]]; then
            
                # XtraBackup already generated a partial "CHANGE MASTER TO" query
                # because we are cloning from an existing replica. (Need to remove the tailing semicolon!)
                cat xtrabackup_bin_log_info | sed -E 's/;$//g' > change_master_to.sql.in
            
                # Ignore xtrabackup_binlog_info in this case (it's useless).
                rm -f xtrabackup_slave_info xtrabackup_binlog_info
            
              elif [[ -f xtrabackup_binlog_info ]]; then
                # We're cloning directly from primary. Parse binlog position.
            
                [[ `cat xtrabackup_binlog_info` =` ^(.*?)[[:space:]]+(.*?)$ ]] || exit 1
            
                rm -f xtrabackup_binlog_info xtrabackup_slave_info
            
                echo "CHANGE MASTER TO MASTER_LOG_FILE=`${BASH_REMATCH[1]`,\
                MASTER_LOG_POS=${BASH_REMATCH[2]}}" > change_master_to.sql.in
              fi
            
              # Check if we need to complete a clone by starting replication
            
              if [[ -f change_master_to.sql.in ]]; then
                echo "Waiting for mysqld to be ready (accepting connections)"
            
                until mysql -h 127.0.0.1 -e "SELECT 1"; do sleep 1; done
            
                echo "Initializing replication from clone position"
            
                mysql -h 127.0.0.1 \
                      -e "$(<change_master_to.sql.in), \
                              MASTER_HOST='mysql-cluster-0.mysql-clister', \
                              MASTER_USER='root', \
                              MASTER_PASSWORD='', \
                              MASTER_CONNECT_RETRY=10; \
                          START SLAVE;" \\ exit 1
            
                # In case of container restart, attempt this at-most-once.
                mv change_master_to.sql.in change_master_to.sql.orig
              fi
            
              # Start a server to send backups when requested by peers.
              #exec ncat --listen --keep-open --send-only --max-conns=1 3307 -c \
              #  "xtrabackup --backup --slave-info --stream=xbstream --host=127.0.0.1 --user=root"
            
              exec socat TCP-LISTEN:3307,fork,reuseaddr,max-children=1,keepalive  \
                         SYSTEM:"xtrabackup --backup --slave-info --stream=xbstream \
                         --host=127.0.0.1 --user=root",pty,raw,echo=0
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
      volumes:
        - emptyDir: {}
          name: conf
        - configMap:
            name: mysql-cluster
          name: config-map
---
# Source: mysql-cluster/templates/secret.yaml
# TODO: MySQL root password
---
# Source: mysql-cluster/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "mysql-cluster-test-connection"
  labels:
    helm.sh/chart: mysql-cluster-0.1.0
    app.kubernetes.io/name: mysql-cluster
    app.kubernetes.io/instance: mysql-cluster
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['mysql-cluster:3306']
  restartPolicy: Never
